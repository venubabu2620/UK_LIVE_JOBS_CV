import {getCurrentUser} from "@/lib/auth"; import {db} from "@/lib/db"; import {redirect} from "next/navigation";
export default async function Admin(){const u=await getCurrentUser();if(!(u as any)?.role||((u as any).role!=="ADMIN"))redirect("/");const [users,jobs,active,last]=await Promise.all([db.user.count(),db.job.count(),db.job.count({where:{isActive:true}}),db.jobSyncRun.findFirst({orderBy:{startedAt:"desc"}})]);return <main className="wrap"><h1>Admin</h1><div className="grid"><div className="card"><h3>Users</h3><b>{users}</b></div><div className="card"><h3>Jobs</h3><b>{jobs}</b></div><div className="card"><h3>Active jobs</h3><b>{active}</b></div></div><div className="card" style={{marginTop:18}}><h3>Latest Adzuna sync</h3><p>{last?`${last.status} — ${last.startedAt.toLocaleString("en-GB")} — created ${last.created}, updated ${last.updated}, skipped ${last.skipped}`:"No sync run yet."}</p></div></main>}
